---
import { useSanityClient } from "astro-sanity";
import Layout from "../components/layout.astro";
import MainHeader from "../components/main-header.astro";
import { sanityPortableText } from "../../utilities/portable-text";

const data = await useSanityClient().fetch(`*[_type == "contact"][0]`);
console.log(data);
---

<Layout title="Contact | Tim Nees Architects" bodyClass="body__contact">
  <div class="backdrop">
    <MainHeader showLogo={true} />
    <section class="contact-text">
      <Fragment
        class:list="profile-text__list"
        set:html={sanityPortableText(data?.content)}
      />
    </section>
  </div>
</Layout>

<style lang="scss" is:global>
  .body__contact::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: url("/Contact background.jpg");
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    filter: grayscale(100%);
    z-index: -1;
  }

  .contact-text > * {
    margin-block: 1rem;
  }
</style>

<style>
  .contact-text {
    display: flex;
    flex-direction: column;
    margin-inline: 3.5rem;
    text-align: left;
    flex-grow: 1;
    max-width: 125ch;
  }
</style>
